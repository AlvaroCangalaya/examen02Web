const mysql = require('mysql2');

// Definir la conexión y configuración de la tabla de Especialidades en MySQL
const db = require('../db');

const EspecialidadSchema = {
    nombre_especialidad: String
};

module.exports = db;
